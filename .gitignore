# build artifacts / binaries
/dist/
/awtest
/worker-bin/
.last-image

# agent-toolchain: multi-GB pre-downloaded build artifacts (rebuilt by
# agent-toolchain/build-toolchain.sh -> fetch-artifacts.sh); never committed.
/agent-toolchain/cache/

# go
go.work.sum
*.db
*.test

# os / editor
.DS_Store
*.swp
.idea/
.vscode/
